<!DOCTYPE html>
<html lang="en">
	<%- include('partials/head') %>

	<body>
		<%- include('partials/header', { active: 'new' }) %>

		<div class="container mt-4 d-flex flex-column gap-4">
			<div class="card">
				<div class="card-body">
					<%- include('partials/product-form') %>
				</div>
			</div>

			<div class="card">
				<div class="card-body">
					<%- include('partials/import-form') %>
				</div>
			</div>
		</div>
	</body>
</html>
